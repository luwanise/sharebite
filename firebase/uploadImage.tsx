export async function uploadImage(uri: string) {
    // This function should have been implemented to upload the image and return a url to the image in firestore
    // but I messed up in creating the firestore and so I can't implement that without billing
    // so for now I'm just returning the image path back
    return uri;
}